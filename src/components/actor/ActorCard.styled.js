import styled from 'styled-components';
import { SearchCard } from '../styled';

const StyledActorCard = styled(SearchCard)`
  .deathday {
    margin: 0;
    margin-top: 15px;
    font-weight: bold;
  }
`;

export default StyledActorCard;
// export const StyledShowCards = styled(SearchCard);
